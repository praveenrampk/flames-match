// Import the flamesMatch function from the src directory.
// This function contains the core logic for calculating the FLAMES relationship.
const flamesMatch = require("./src/flames");

// Export the flamesMatch function as the main export of the package.
// This allows users to import your package and directly use the function.
module.exports = flamesMatch;
